services:
  - type: web
    name: happytracker-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install --legacy-peer-deps
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3001"
      - key: DATABASE_URL
        value: your_postgresql_database_url_here
  
  - type: static
    name: happytracker-frontend
    plan: free
    rootDir: apps/web
    buildCommand: npm install --legacy-peer-deps && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_HUGGINGFACE_TOKEN
        value: your_huggingface_token_here
      - key: VITE_MESHY_API_KEY
        value: your_meshy_api_key_here
      - key: VITE_API_URL
        value: https://happytracker-backend.onrender.com/api